# =============================================================================
# SalesFlow CRM - Environment Configuration Template
# =============================================================================
# Copy this file to .env.local and update with your actual values
# Never commit .env.local to version control!

# =============================================================================
# 🚀 Application Configuration
# =============================================================================

# Application URL (used for redirects and API calls)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment (development, production, staging)
NODE_ENV=development

# =============================================================================
# 🔗 Supabase Configuration
# =============================================================================

# Your Supabase project URL (get from Supabase Dashboard > Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (get from Supabase Dashboard > Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase service role key (optional - for server-side operations)
# WARNING: Keep this secret! Only use on server-side
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# 🤖 AI Integration (Optional)
# =============================================================================

# Google Gemini API Key (for AI features like email composer and deal insights)
# Get from: https://makersuite.google.com/app/apikey
NEXT_PUBLIC_GOOGLE_AI_API_KEY=your-gemini-api-key-here

# OpenAI API Key (alternative to Gemini)
OPENAI_API_KEY=your-openai-api-key-here

# =============================================================================
# 📧 Email Configuration (Optional)
# =============================================================================

# SMTP configuration for sending emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# SendGrid API Key (alternative to SMTP)
SENDGRID_API_KEY=SG.your-sendgrid-api-key

# =============================================================================
# 📊 Analytics & Monitoring (Production)
# =============================================================================

# Vercel Analytics (if deploying to Vercel)
NEXT_PUBLIC_VERCEL_ANALYTICS=true

# Sentry DSN for error tracking
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# PostHog for product analytics
NEXT_PUBLIC_POSTHOG_KEY=your-posthog-key
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Google Analytics ID
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# =============================================================================
# 🔒 Security Configuration (Production)
# =============================================================================

# JWT secret for additional token signing (if needed)
JWT_SECRET=your-super-secret-jwt-key-here

# API rate limiting (requests per minute)
API_RATE_LIMIT=100

# Security webhook URL for critical event alerts
SECURITY_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Session security configuration
SESSION_SECRET=your-session-secret-here
SESSION_MAX_AGE=86400
SESSION_IDLE_TIMEOUT=1800

# =============================================================================
# 🗄️ External Integrations (Optional)
# =============================================================================

# Calendar integration (Google Calendar, Outlook, etc.)
GOOGLE_CALENDAR_CLIENT_ID=your-google-calendar-client-id
GOOGLE_CALENDAR_CLIENT_SECRET=your-google-calendar-client-secret

# File storage (if not using Supabase storage)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_BUCKET_NAME=your-bucket-name

# =============================================================================
# 🧪 Development & Testing
# =============================================================================

# Database URL for local development (optional)
DATABASE_URL=postgresql://username:password@localhost:5432/salesflow_dev

# Test database URL
TEST_DATABASE_URL=postgresql://username:password@localhost:5432/salesflow_test

# Disable telemetry for development
NEXT_TELEMETRY_DISABLED=1

# =============================================================================
# 📝 Configuration Notes
# =============================================================================
# 
# Required Variables (minimum for app to work):
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - NEXT_PUBLIC_APP_URL
# 
# Optional but Recommended:
# - NEXT_PUBLIC_GOOGLE_AI_API_KEY (for AI features)
# - NEXT_PUBLIC_SENTRY_DSN (for error tracking in production)
# 
# Production Only:
# - SUPABASE_SERVICE_ROLE_KEY
# - JWT_SECRET
# - SMTP configuration
# 
# =============================================================================